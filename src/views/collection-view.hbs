{{> menu active="dashboard" }}

<section class="section" id="map-section">
  <div class="card">
    <div class="card-content">
      <div class="content">
        <h4 class="title is-4">Map</h4>
        {{> map}}
      </div>
    </div>
  </div>
</section>

<section class="section">
  <h1 class="title">
    {{collection.name}}
  </h1>
  {{> list-placemarks collection=collection }}
  {{> add-placemark}}
</section>